import React from 'react'
import * as Styled from './styles'

export const Header = () => (
  <Styled.HeaderContainer>
    <Styled.Title>
      Weather web
    </Styled.Title>
  </Styled.HeaderContainer>
)